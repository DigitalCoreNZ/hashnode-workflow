on:
    push:
    repository_dispatch:
    types: [trigger]

jobs:
    print_file_job:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v4
        with:
            fetch-depth: 0
